---
- hosts: kali
  become: yes
  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes
    - name: Install attack tools
      apt:
        name:
          - snort
          - wireshark
          - nmap
        state: present
