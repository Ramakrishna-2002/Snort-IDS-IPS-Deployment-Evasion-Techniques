---
- hosts: snort
  become: yes
  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes
    - name: Install Snort and tools
      apt:
        name:
          - snort
          - wireshark
          - nmap
        state: present
